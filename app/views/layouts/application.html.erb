<!DOCTYPE html>
<html>
  <head>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <!-- Connect Bootstrap JavaScript and its dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>

    <title>Fantasy Forger</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" %>

    <style>
      body {
        background-color: black;
        color: white;
        Font-family: "Montserrat";
        font-smooth: always;
        font-size: 17px;
      }
      .health-bar{
        color: red;
      }
      .collapse-content {
        background-color: red;
        color: white;
        
      }

      .navbar {
        background-color: #AA0000;
        color: white;
        
      }

      .btn-secondary {
        margin-bottom: 20px;
        background-color: red;
        color: white;
        width: 200px;
        height: 70px;
        display: flex;
        align-items: center;
        justify-content: center;

      }

      .btn-primary {
        background-color: red;
        color: white;
        width: 200px;
        height: 70px;
        display: flex;
        align-items: center;
        justify-content: center;

      }

      .navbar-toggler-icon span {
        background-color: white;
        
      }

      .modal-header {
        background-color: red;
      }

      .modal-body {
        background-color: black;
      }

      .modal-footer {
        background-color: black;
      }

      #land-body{
        margin-top: 100px;
        margin-left: 500px;
        margin-right: 500px;
        font-size: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      #details {
        background-color: red;
        color: white;


      }
      @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
          transform: translateY(0);
        }
        40% {
          transform: translateY(-30px);
        }
        60% {
          transform: translateY(-15px);
        }
      }

      .bounce {
        animation: bounce 0.5s;
      }
    </style>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        var levelUpIcon = document.getElementById('levelUpIcon');

        levelUpIcon.addEventListener('click', function () {
          levelUpIcon.classList.add('bounce');
          setTimeout(function () {
            levelUpIcon.classList.remove('bounce');
          }, 1000); // Adjust the duration of the bounce animation
        });
      });
    </script>
  </head>

  <body>
    <div class="navbar text-center">
      <h1 class="text-white mx-auto">Fantasy Forger</h1>
      <span>
        <% if user_signed_in? %>
          <div class="btn-group">
            <%= button_to "Edit", edit_user_registration_path, method: :get, class: "btn btn-tertiary" %>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "btn btn-tertiary" %>
          </div>
        <% else %>
          <div class="btn-group">
            <%= link_to "Sign In", new_user_session_path, class: "btn btn-tertiary" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-tertiary" %>
          </div>
        <% end %>
      </span>
    </div>


  <%= yield %>
</body>
</html>
