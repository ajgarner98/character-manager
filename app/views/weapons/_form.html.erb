<%= form_with(model: [@character, @weapon], local: true) do |form| %>
  <% if weapon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weapon.errors.count, "error") %> prohibited this weapon from being saved:</h2>

      <ul>
        <% weapon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :damage, style: "display: block" %>
    <%= form.text_field :damage %>
  </div>

  <div>
    <%= form.label :weapon_type, style: "display: block" %>
    <%= form.text_field :weapon_type %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :character_id, style: "display: block" %>
    <%= form.hidden_field :character_id, value: @character.id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
